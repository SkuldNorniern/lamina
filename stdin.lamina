# Test stdin read operations
fn @main() -> i64 {
    entry:
        # Read first byte (should be 'A' = 65)
        %byte1 = readbyte
        print %byte1

        # Read second byte (should be newline = 10 from echo input)
        %byte2 = readbyte
        print %byte2

        # Read third byte (should be 'B' = 66)
        %byte3 = readbyte
        print %byte3

        # Return the first byte value
        ret.i64 %byte1
}













